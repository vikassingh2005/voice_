## 2024-11-20 - Missing ARIA Labels on Icon Buttons
**Learning:** Custom UI layouts relying heavily on SVG icons often omit basic accessibility attributes, rendering critical interactive elements like microphones and send buttons invisible to screen readers.
**Action:** Always scan icon-only buttons for missing `aria-label` or `title` attributes during initial UX audits.
